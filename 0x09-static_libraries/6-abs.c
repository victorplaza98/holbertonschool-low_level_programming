#include "holberton.h"
/**
 * _abs - Returns value of n
 * @n: integer value
 * Description: The program value of a number
 * Return: n
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
